import React from 'react';

export default function Logo({className}) {
    return (
        <img
            className={className}
            src="/assets/logo-white.svg"
            alt="logo"
        />
    )
}
